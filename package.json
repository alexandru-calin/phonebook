{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon",
    "build:ui": "rm -rf dist && cd client && npm install && npm run build && mv dist ..",
    "deploy:full": "git add client && git commit -m uibuild && git push"
  },
  "keywords": [],
  "author": "alexandru calin",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "mongoose": "^8.2.0",
    "morgan": "^1.10.0"
  }
}